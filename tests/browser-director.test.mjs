import test from 'node:test';
import assert from 'node:assert/strict';
import { buildIssueDraft, callBrowserLlm, localCommandReply, validateBrowserProviderUrl } from '../lib/browser-director.mjs';

test('browser status reports LR0 through LR3 closed with LR4 active',()=>{const r=localCommandReply('/status');assert.match(r.reply,/LR0 bis LR3 sind geschlossen/);assert.match(r.reply,/Das Zimmer/);assert.match(r.reply,/9\/9 Stationen/);assert.match(r.reply,/LR4 Selected-Pilot-Fire-Test/);assert.match(r.reply,/Issue #76/);assert.match(r.reply,/\/studio\//);assert.doesNotMatch(r.reply,/100 ?%/);});
test('browser next protects selected-pilot fire test',()=>{const r=localCommandReply('/next');assert.match(r.reply,/SelectedPilotEpisodePackage-Vertrag/);assert.match(r.reply,/REVIEW_REQUIRED/);assert.match(r.reply,/Zustandslöschung/);assert.match(r.reply,/hashgleichen Restore/);assert.match(r.reply,/Kein Blind-Merge/);assert.match(r.reply,/keine Bildgenerierung/i);});
test('browser characters reflect selected pilot but open detail gates',()=>{const r=localCommandReply('/characters');assert.match(r.reply,/ausgewählten Pilotlinie Das Zimmer/);assert.match(r.reply,/offenem Review/);assert.match(r.reply,/Visuelle Master bleiben 0\/4/);assert.match(r.reply,/keine Detail-, Visual- oder Voice-Freigabe/);});
test('browser plan is LR0 through LR6',()=>{const r=localCommandReply('/plan');for(const marker of ['LR0','LR1','LR2','LR3','LR4','LR5','LR6'])assert.match(r.reply,new RegExp(marker));assert.match(r.reply,/LR3 neutraler Studio-bis-Restore-Loop öffentlich bewiesen/);assert.match(r.reply,/LR4 Das-Zimmer-Fire-Test aktiv/);});
test('issue draft carries LR4 and strict scope',()=>{const url=new URL(buildIssueDraft({type:'WORK PACKET',title:'SelectedPilotEpisodePackage definieren'}));const body=url.searchParams.get('body');assert.equal(url.pathname,'/Pagebabe/comic/issues/new');assert.match(body,/Issue #76/);assert.match(body,/LR0 bis LR3: geschlossen/);assert.match(body,/9\/9/);assert.match(body,/REVIEW_REQUIRED/);assert.match(body,/Kein Growth OS/i);assert.match(body,/Keine Bildgenerierung/i);assert.match(body,/Pagebabe\/comic/);});
test('render draft is visibly blocked',()=>{const r=localCommandReply('/render shot-1');assert.match(r.reply,/LR4/);assert.match(r.reply,/Visual-Locks/);assert.doesNotMatch(r.reply,/LR3, LR4/);assert.match(r.issueDraftUrl,/github\.com/);});
test('provider validation stays HTTPS and allowlisted',()=>{assert.equal(validateBrowserProviderUrl('http://integrate.api.nvidia.com/v1'),null);assert.equal(validateBrowserProviderUrl('https://evil.example/v1'),null);assert.equal(validateBrowserProviderUrl('https://api.openai.com/v1'),'https://api.openai.com/v1');});
test('browser LLM receives LR4 truth context',async()=>{let captured;const reply=await callBrowserLlm({message:'Audit',history:[],apiKey:'k',baseUrl:'https://api.openai.com/v1',model:'m',fetchImpl:async(url,options)=>{captured=JSON.parse(options.body);return{ok:true,json:async()=>({choices:[{message:{content:'ok'}}]})};}});assert.equal(reply,'ok');const system=captured.messages[0].content;assert.match(system,/RECOVERY LINE ACTIVE/);assert.match(system,/aktives Gate: LR4 Selected-Pilot-Fire-Test/);assert.match(system,/Issue #76/);assert.match(system,/9\/9 Stationen/);assert.match(system,/Studio-Route/);assert.match(system,/keine aktuelle Prozentzahl/i);});
